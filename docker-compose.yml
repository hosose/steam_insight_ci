services:
  was-service:
    build:
      context: ./apps/was
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped

  web:
    build:
      context: ./apps/web
    ports:
      - "3000:80"
    depends_on:
      - was-service
    restart: unless-stopped
